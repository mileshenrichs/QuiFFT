language: java

sudo: false

install:
  - mvn install -DskipTests=true -Dgpg.skip=true

script: 
- ls -alFt
- gpg --import exported_key.gpg
- gpg --import exported_secret_key.asc
- mvn cobertura:cobertura

after_success:
  - bash <(curl -s https://codecov.io/bash)